<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="wifiCrack.Views.DisclaimerPage"
             Title="Avertissement Légal"
             BackgroundColor="{StaticResource BackgroundColor}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <!-- Icône et titre -->
            <Label Text="⚖️"
                   FontSize="60"
                   HorizontalOptions="Center"/>

            <Label Text="WiFi Security Auditor"
                   FontSize="28"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>

            <Label Text="Outil Éducatif d'Audit de Sécurité"
                   FontSize="16"
                   HorizontalOptions="Center"
                   Opacity="0.7"/>

            <!-- Ligne de séparation -->
            <BoxView HeightRequest="1"
                     Color="{StaticResource Gray300}"
                     Margin="0,10"/>

            <!-- Avertissement principal -->
            <Frame BackgroundColor="{StaticResource Warning}"
                   Padding="15"
                   CornerRadius="10"
                   HasShadow="True">
                <VerticalStackLayout Spacing="10">
                    <Label Text="⚠️ AVERTISSEMENT IMPORTANT"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="White"/>

                    <Label TextColor="White">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="Cette application est destinée à des fins ÉDUCATIVES et de RECHERCHE uniquement. L'utilisation de cet outil pour tester la sécurité de réseaux WiFi qui ne vous appartiennent pas est "/>
                                <Span Text="ILLÉGALE" FontAttributes="Bold"/>
                                <Span Text=" et passible de sanctions pénales."/>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                </VerticalStackLayout>
            </Frame>

            <!-- Cadre légal -->
            <Label Text="📋 Cadre Légal"
                   FontSize="20"
                   FontAttributes="Bold"
                   Margin="0,10,0,5"/>

            <Frame BorderColor="{StaticResource Primary}"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="8">
                    <Label Text="🇫🇷 Code Pénal Français - Article 323-1"
                           FontAttributes="Bold"/>

                    <Label Text="L'accès frauduleux à un système de traitement automatisé de données est puni de :"
                           FontSize="14"/>

                    <Label Text="• Jusqu'à 2 ans de prison"
                           FontSize="14"
                           Margin="10,0,0,0"/>

                    <Label Text="• 60 000€ d'amende"
                           FontSize="14"
                           Margin="10,0,0,0"/>

                    <Label Text="• Peines aggravées en cas de suppression ou modification de données"
                           FontSize="14"
                           Margin="10,0,0,0"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Utilisations autorisées -->
            <Label Text="✅ Utilisations Autorisées"
                   FontSize="20"
                   FontAttributes="Bold"
                   Margin="0,10,0,5"/>

            <VerticalStackLayout Spacing="8">
                <Label Text="• Tester VOTRE propre réseau WiFi"/>
                <Label Text="• Audit avec autorisation ÉCRITE du propriétaire"/>
                <Label Text="• Recherche en environnement contrôlé"/>
                <Label Text="• Formation et éducation"/>
                <Label Text="• Compétitions CTF et challenges de sécurité"/>
            </VerticalStackLayout>

            <!-- Utilisations interdites -->
            <Label Text="❌ Utilisations Interdites"
                   FontSize="20"
                   FontAttributes="Bold"
                   Margin="0,10,0,5"/>

            <VerticalStackLayout Spacing="8">
                <Label Text="• Scanner des réseaux qui ne vous appartiennent pas"
                       TextColor="{StaticResource Danger}"/>
                <Label Text="• Tenter de se connecter sans autorisation"
                       TextColor="{StaticResource Danger}"/>
                <Label Text="• Capturer du trafic réseau d'autrui"
                       TextColor="{StaticResource Danger}"/>
                <Label Text="• Exploiter des vulnérabilités découvertes"
                       TextColor="{StaticResource Danger}"/>
            </VerticalStackLayout>

            <!-- Engagements -->
            <Label Text="📝 Vos Engagements"
                   FontSize="20"
                   FontAttributes="Bold"
                   Margin="0,10,0,5"/>

            <Frame BackgroundColor="{StaticResource LightBackground}"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="En utilisant cette application, vous vous engagez à :"/>

                    <Label Text="1. N'utiliser cet outil que dans un cadre légal"/>
                    <Label Text="2. Obtenir une autorisation écrite avant tout test"/>
                    <Label Text="3. Respecter la vie privée et les données d'autrui"/>
                    <Label Text="4. Signaler les vulnérabilités de manière responsable"/>
                    <Label Text="5. Assumer l'entière responsabilité de vos actions"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Checkbox d'acceptation -->
            <VerticalStackLayout Spacing="10" Margin="0,20,0,0">
                <HorizontalStackLayout Spacing="10">
                    <CheckBox x:Name="AcceptCheckBox"
                              VerticalOptions="Center"/>
                    <Label VerticalOptions="Center">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="J'ai lu et j'accepte les conditions d'utilisation. Je m'engage à utiliser cet outil de manière "/>
                                <Span Text="légale et éthique" FontAttributes="Bold"/>
                                <Span Text=" uniquement."/>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                </HorizontalStackLayout>

                <Button x:Name="AcceptButton"
                        Text="Accepter et Continuer"
                        Clicked="OnAcceptClicked"
                        IsEnabled="{Binding Source={x:Reference AcceptCheckBox}, Path=IsChecked}"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White"
                        CornerRadius="10"
                        Padding="15"
                        Margin="0,10"/>

                <Button Text="Refuser et Quitter"
                        Clicked="OnDeclineClicked"
                        BackgroundColor="{StaticResource Gray300}"
                        TextColor="{StaticResource Gray900}"
                        CornerRadius="10"
                        Padding="15"/>
            </VerticalStackLayout>

            <!-- Footer -->
            <Label Text="Version 2.0 - À des fins éducatives uniquement"
                   FontSize="12"
                   Opacity="0.6"
                   HorizontalOptions="Center"
                   Margin="0,20,0,0"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
